# Production Environment Configuration

# Disable admin mockup features in production
VITE_ENABLE_ADMIN=false

# API Base URL (without /api suffix, will be added by api.js - update for production server)
VITE_API_URL=http://127.0.0.1:8000
